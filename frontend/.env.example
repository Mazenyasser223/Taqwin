# Copy to .env.local for local overrides. Never commit .env or .env.local.

# Backend API base URL (no trailing slash). Production example:
# VITE_API_URL=https://taqwin.onrender.com
VITE_API_URL=http://localhost:4000
