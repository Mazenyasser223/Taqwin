# Taqwin — Frontend

React web application for Trainee, Trainer, and Gym Owner dashboards.

- **Stack:** React (Vite), TypeScript
- **UI:** Light / dark mode, Taqwin design system

Setup: `npm install` → `npm run dev`
