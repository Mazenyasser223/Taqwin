# Taqwin — Backend (Node.js)

Main API: auth, users, gyms, bookings, marketplace, notifications.

- **Stack:** Node.js, (framework TBD)
- **Database:** Designed from scratch (schema in repo docs)

Setup: `npm install` → `npm run dev`
