# Taqwin — AI Service (FastAPI)

AI engine: body composition, nutrition analysis, Gemini coach.

- **Stack:** Python, FastAPI
- **AI:** Google Gemini API

Setup: `pip install -r requirements.txt` → `uvicorn main:app --reload`
